apiVersion: v1
kind: Secret
metadata:
  name: kruize-db-credentials
  namespace: openshift-tuning
  labels:
    app.kubernetes.io/name: kruize-db
    app.kubernetes.io/component: database
type: Opaque
stringData:
  # These are placeholder values - actual secrets should be pre-provisioned
  # by the tenant admin before deployment
  POSTGRES_USER: kruize
  POSTGRES_PASSWORD: CHANGE_ME_BEFORE_DEPLOYMENT
  POSTGRES_DB: kruizedb

