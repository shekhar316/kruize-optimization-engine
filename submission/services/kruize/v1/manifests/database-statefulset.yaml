apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kruize-db
  namespace: openshift-tuning
  labels:
    app.kubernetes.io/name: kruize-db
    app.kubernetes.io/component: database
spec:
  serviceName: kruize-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kruize-db
  template:
    metadata:
      labels:
        app: kruize-db
        app.kubernetes.io/name: kruize-db
        app.kubernetes.io/component: database
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: postgres
        image: quay.io/kruizehub/postgres:15.2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5432
          name: postgres
          protocol: TCP
        env:
        - name: POSTGRES_PASSWORD
          value: admin
        - name: POSTGRES_USER
          value: admin
        - name: POSTGRES_DB
          value: kruizeDB
        - name: POSTGRESQL_PASSWORD
          value: admin
        - name: POSTGRESQL_USER
          value: admin
        - name: POSTGRESQL_DATABASE
          value: kruizeDB
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          # Let OpenShift assign UID automatically
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 1000m
            memory: 2Gi
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U $POSTGRES_USER -d $POSTGRES_DB
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U $POSTGRES_USER -d $POSTGRES_DB
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
      labels:
        app.kubernetes.io/name: kruize-db
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 5Gi

